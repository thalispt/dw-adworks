version: 2

models:
  - name: dim_sales_reason
    description: The dimension with types of sales reason.
    columns:
      - name: reason_sk
        description: The primary key of salesreason dimension.
        tests:
          - unique
          - not_null

      - name: salesreasonid
        description: Salesreason primary key.
      
      - name: reason_name
        description: Sales reason description.

      - name: reasontype
        description: Reason type.